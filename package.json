{
  "name": "@bloom-directory/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vocs dev",
    "lint": "tsc --noEmit && node scripts/check-status-badges.mjs",
    "build": "vocs build && node scripts/copy-markdown-routes.mjs",
    "preview": "vocs preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "waku": "1.0.0-rc.0"
  },
  "devDependencies": {
    "typescript": "latest"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "fsevents@2.3.3": true
  }
}
