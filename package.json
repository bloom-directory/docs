{
  "name": "@bloom-directory/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vocs dev",
    "build": "vocs build && node scripts/postbuild.mjs",
    "preview": "vocs preview",
    "cf:dev": "npm run build && wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "^8.0.16",
    "vocs": "^2.0.12",
    "waku": "1.0.0-beta.1"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "^4.84.1"
  }
}
